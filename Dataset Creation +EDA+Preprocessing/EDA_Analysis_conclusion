1. There are no missing data across all the features in the data
2. There are no duplicate rows in the data
3. We are gonna used Tree-based Algos, to prevent any scaling-related issues
4. N=∣n_estimators∣×∣learning_rate∣×∣max_depth∣×∣subsample∣×∣colsample_bytree∣×∣gamma∣×∣reg_alpha∣×∣reg_lambda∣

Total time:

T=N×t
single_fit
	​


where t
single_fit
	​

 = time to train one XGB model.

Example:

If each list has:

n_estimators → 5
learning_rate → 4
max_depth → 5
subsample → 3
colsample_bytree → 3
gamma → 4
reg_alpha → 4
reg_lambda → 4

Models:

5×4×5×3×3×4×4×4=57,600

If one fit takes:

0.5 sec → 8 hours
2 sec → 32 hours
10 sec → ~6.6 days

So brute-force explodes extremely fast.

5. 